version: '3.8'

services:
  health-monitor:
    build: .
    env_file:
      - .env
    ports:
      - "10000:10000" # 修复：代码里 Flask 用的是 10000 端口，这里要保持一致
    volumes:
      - ./.env:/app/.env
    restart: always
